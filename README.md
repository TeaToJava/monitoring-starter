После подключения зависимости к проекту, 
добавить следующие параметры:
app.monitoring.logging.enabled=true
app.monitoring.logging.minLoggingInterval= (минимальное время (в миллисекундах) для логирования)
